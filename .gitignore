# Secrets. The API key must never be committed.
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.python-version

# Tooling
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Logs and OS noise
*.log
.DS_Store

# NOTE: .secrets.baseline is deliberately NOT ignored - detect-secrets needs it
# committed so the pre-commit hook only flags new findings.
